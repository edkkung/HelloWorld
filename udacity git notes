this is alternate version?!


cd ~                          # change directories to your home directory
mkdir version-control         # make version-control directory
cd version-control            # go to version-control directory
mkdir reflections             # create reflections directory
cd reflections                # go to reflections directory
subl lesson_1_reflections.txt # launch sublime with file called lesson_1_reflections.txt (you can replace subl with another editor here if you prefer a different one)
open -e .bash_profile

git log --status			# check compare versions
git log --graph --oneline b1 b2
https://git-scm.com/downloads # install git

git checkout				# restore previous version 
git diff -u old new			# If no file specified, compare WD to Staging, " --staged" for staging to repo
git show new				# If you dont know the parent
git clone
git log



git config --global core.editor "'/Applications/Sublime Text.app/Contents/SharedSupport/bin/subl' -n -w"
git config --global push.default upstream
git config --global merge.conflictstyle diff3
git config --global color.ui auto # change color difference
git config --global user.name "Your Name"

#lesson 2
git init					# initialize and create .git in cd, however have to manual 1st commit else EMPTY
git status					# what has changed since last, including untracked in wd 
# WD -> Staging -> GIT
git add file1				# now in staging
git add file2				# run git status, reveal both file1 and file2 ready to be committed
git reset file2				# remove from git, still in wd
git commit -m "message"		# commit
git branch					# check how many branches
git branch new_name			# create new branch. then run git checkout and begin working from there
git merge master b1			# merge
git branch -d b1			# delete the branch (label) since dont need after merging

git gc 						# garbage collection, remove unreachable branch commit post merge

# lesson 3
git remote						
git remote add origin(name) url	# create "branch"
git remote -v 				# verbose
git push origin master		# master (local) will go to origin (remote)



-OK lets try something new change-
